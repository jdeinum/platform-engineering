# Github Actions

Currently, I think the best way to go about this would be to setup some example
actions files for different scenarios. I could give an example of:

1. what testing a simple rust application looks like, easy to use caching +
   matrix builds (i.e toolchains in rust) + deploying image + trivy scanning
2. linting / testing / validating / scanning open tofu / pulumi / bicep code (1 for each i
   assume)
3.
