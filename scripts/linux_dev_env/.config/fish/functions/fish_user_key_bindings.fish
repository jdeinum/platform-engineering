function fish_user_key_bindings
    bind -s --preset -M insert ctrl-p up-or-search
    bind -s --preset -M insert ctrl-n down-or-search
    bind -s --preset -M insert ctrl-space accept-autosuggestion
end
